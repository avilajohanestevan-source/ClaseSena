# Dependencias de npm: se instalan con `npm install` y se copian con `npm run vendor`.
/node_modules/

# Fotos de daños subidas desde la app (se crean al usar la inspección).
/uploads/

# Dependencias PHP (PhpSpreadsheet): se instalan con `composer install`.
/api/vendor/
