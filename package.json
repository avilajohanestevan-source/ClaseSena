{
  "name": "sena-ambientes-front",
  "private": true,
  "type": "module",
  "description": "Front-end de asistencia y gestión de ambientes SENA. `npm install` las descarga y `npm run vendor` copia las versiones minificadas a vendor/ (que sí se sube al repositorio).",
  "scripts": {
    "vendor": "node scripts/vendor.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@zxing/library": "^0.21.3",
    "gsap": "^3.13.0"
  }
}
